<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:gravity="center_vertical"
            android:text="@string/app_name"
            android:textSize="26sp"
            android:textColor="@color/text_primary" />

        <!-- 开机启动 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/set_boot_title"
            android:textSize="22sp"
            android:textColor="@color/text_primary" />

        <Switch
            android:id="@+id/bootSwitch"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:textSize="20sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/set_boot_desc"
            android:textSize="16sp"
            android:textColor="@color/text_secondary" />

        <!-- 直播源 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/set_source_title"
            android:textSize="22sp"
            android:textColor="@color/text_primary" />

        <EditText
            android:id="@+id/urlInput"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="8dp"
            android:inputType="textUri"
            android:textSize="18sp"
            android:hint="@string/set_source_hint"
            android:textColor="@color/text_primary"
            android:background="#222222" />

        <Button
            android:id="@+id/btnUpdate"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:layout_marginTop="12dp"
            android:text="@string/set_btn_update"
            android:textSize="22sp"
            android:textColor="@color/text_primary"
            android:background="#AA335577FF" />

        <Button
            android:id="@+id/btnImport"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:layout_marginTop="12dp"
            android:text="@string/set_btn_import"
            android:textSize="22sp"
            android:textColor="@color/text_primary"
            android:background="#AA335577FF" />

        <TextView
            android:id="@+id/countText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/set_count"
            android:textSize="18sp"
            android:textColor="@color/text_secondary" />

        <Button
            android:id="@+id/btnClear"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:layout_marginTop="16dp"
            android:text="@string/set_btn_clear"
            android:textSize="22sp"
            android:textColor="#FF8888"
            android:background="#33222222" />

        <Button
            android:id="@+id/btnBack"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:layout_marginTop="24dp"
            android:text="@string/set_btn_back"
            android:textSize="22sp"
            android:textColor="@color/text_primary"
            android:background="#AA444444" />

    </LinearLayout>
</ScrollView>
